[develop]
script_dir=$base/lib/arc_experiment_gui
[install]
install_scripts=$base/lib/arc_experiment_gui

[flake8]
max-line-length = 100
# E702: compact `w = QWidget(); w.setX(...)` widget-init style is used deliberately
extend-ignore = E702
